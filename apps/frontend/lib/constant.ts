export enum Gender {
  MALE = "male",
  FEMALE = "female",
}

export enum Route {
  HOME = "/",
  SIGNUP = "/signup",
  SIGNIN = "/signin",
  PROFILE = "/profile",
}

export enum ApiRoute {
  USER = "/users",
  CREATE_USER = "/users",
  UPDATE_USER = "/users",
  SIGNIN = "/auth/signin",
  PRODUCT = "/products",
}
